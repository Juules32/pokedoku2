name: Deploy Backend To DigitalOcean

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

env:
  DOCKER_REPOSITORY: "pokedoku2-backend-repository"
  IMAGE_NAME: "pokedoku2"

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4
        
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_REPOSITORY }}
  deploy:
    runs-on: ubuntu-latest
    needs: build_and_push
    steps:
      - name: Deploy to Digital Ocean droplet via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.BACKEND_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            # Stop running container
            docker stop ${{ env.IMAGE_NAME }} || true
            # Remove old container
            docker rm ${{ env.IMAGE_NAME }} || true
            # Pull the new image
            docker pull ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_REPOSITORY }}
            # Run the new container
            docker run -d \
              --restart always \
              --name ${{ env.IMAGE_NAME }} \
              -e POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }} \
              -e FRONTEND_HOST=${{ secrets.FRONTEND_HOST }} \
              -p 80:8000 \
              ${{ secrets.DOCKER_USERNAME }}/${{ env.DOCKER_REPOSITORY }}

# Environment variables are added with the -e flag